# behat.yml
default:
    autoload:
        '': %paths.base%/features
    suites:
        default:
          paths:
            features: features
            bootstrap: %behat.paths.features%/bootstrap