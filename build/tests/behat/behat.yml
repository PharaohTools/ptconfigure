# behat.yml
default:
    autoload:
        '': %paths.base%/features/bootstrap
    suites:
        default:
          paths:
            features: features
            bootstrap: %behat.paths.features%/bootstrap