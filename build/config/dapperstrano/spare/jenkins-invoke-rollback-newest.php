<?php

/*************************************
*      Generated Autopilot file      *
*     ---------------------------    *
*Autopilot Generated By Dapperstrano *
*     ---------------------------    *
*************************************/

Namespace Core ;

class AutoPilotConfigured extends AutoPilot {

    public $steps ;
    public $swaps ;

    public function __construct() {
        $this->setSteps();
        $this->setSSHData();
    }

    /* Steps */
    private function setSteps() {

      $this->steps =
        array(
          array ( "InvokeSSH" => array(
            "sshInvokeSSHDataExecute" => true,
            "sshInvokeSSHDataData" => "",
            "sshInvokeServers" => array(
              array("target" => "178.63.72.155", "user" => "1", "pword" => "1", ),

            ),
          ) , ) ,
        );

    }


//
// This function will set the sshInvokeSSHDataData variable with the data that
// you need in it. Call this in your constructor
//
  private function setSSHData() {
    $timeDrop = time();
    $this->steps[0]["InvokeSSH"]["sshInvokeSSHDataData"] = <<<"SSHDATA"
cd /tmp/
git clone -b master --no-checkout --depth 1 https://goldencontact:turbulentDeploy1995@bitbucket.org/goldencontact dapper$timeDrop
cd dapper$timeDrop
git show HEAD:build/config/dapperstrano/autopilots/jenkins-node-install-code.php > /tmp/jenkins-node-install-code.php
rm -rf /tmp/dapper$timeDrop
cd /tmp/
sudo dapperstrano autopilot execute jenkins-node-install-code.php
sudo chown -R www-data /var/www/jenkins/alca-enterprisecurrent/src
sudo rm jenkins-node-install-code.php
SSHDATA;
  }

}
